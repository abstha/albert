# Core deep learning + model hub
torch           # PyTorch, installs CPU wheel by default in Codespaces
transformers    # Hugging Face library (ALBERT, BERT, GPT-2, etc.)
datasets        # Hugging Face datasets (GLUE, SQuAD, etc.)
evaluate        # For metrics (accuracy, F1, etc.)
accelerate      # Training helpers, distributed utils
sentencepiece   # Tokenizer dependency for ALBERT
